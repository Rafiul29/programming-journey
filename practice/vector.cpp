#include <bits/stdc++.h>
using namespace std;
int main() {
  // vector<int> v={1,2,34,3,34,34,34};
  // // v.resize(20);
  // v.resize(20,10);
  // v.push_back(100000);
  // v.pop_back();
  
  // replace(v.begin(),v.end(),34,35);

  // auto it=find(v.begin(),v.end(),800);
  // cout<<*it<<"das";

  // for(int i=0; i<v.size();i++){
  //   cout<<v[i]<<" ";
  // }
  // cout<<endl;
  // cout<<"font value "<<v.front()<<endl;
  // cout<<"back value "<<v.back()<<endl;

  // cout<<v.max_size()<<endl;
  // cout<<v.empty()<<endl;
  int n;
  cin>>n;
  cin.ignore();
  vector<string> v(n);
  string s;
  getline(cin,s);
  cout<<s;
  // for(int i=0;i<n;i++){
  //   // cin>>v[i];
  //    getline(cin,s);
  //   v.push_back(s);
  // }

  // for(int i=0;i<n;i++){
  //   cout<<v[i]<<" ";
  // }

  return 0;
}